# Adding a Scraper

A shared scraper operation should validate its URI, request the document, fail on unsuccessful status, parse HTML, extract one defined structure, and return a predictable Python result.
