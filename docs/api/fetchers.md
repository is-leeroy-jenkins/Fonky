# API — `fetchers.py`

## Classes

| Class | Public Methods | Purpose |
|---|---:|---|
| `Fetcher` | 1 | Fetcher fetcher. Coordinates base fetcher operations within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `WebFetcher` | 20 | Web Fetcher fetcher. Coordinates HTTP web page retrieval and HTML extraction within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `WebCrawler` | 4 | Web Crawler fetcher. Coordinates bounded web crawling and optional browser-rendered scraping within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `ArXiv` | 1 | Ar Xiv fetcher. Coordinates ArXiv research document retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `GoogleDrive` | 4 | Google Drive fetcher. Coordinates Google Drive document retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance. |
| `Wikipedia` | 1 | Wikipedia fetcher. Coordinates Wikipedia document retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `TheNews` | 1 | The News fetcher. Coordinates The News API article retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `GoogleSearch` | 1 | Google Search fetcher. Coordinates Google Custom Search retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `GoogleMaps` | 5 | Google Maps fetcher. Coordinates Google Maps geocoding, address validation, and directions within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `GoogleWeather` | 8 | Google Weather fetcher. Coordinates Google Weather conditions, forecasts, history, and alerts within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `NavalObservatory` | 6 | Naval Observatory fetcher. Coordinates U.S. Naval Observatory celestial-navigation data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `SatelliteCenter` | 4 | Satellite Center fetcher. Coordinates SSC satellite observatory, ground-station, and location data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance. instance. |
| `EarthObservatory` | 6 | Earth Observatory fetcher. Coordinates NASA EONET events, categories, sources, and layers within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `GlobalImagery` | 6 | Global Imagery fetcher. Coordinates NASA GIBS WMS imagery and map service retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `NearbyObjects` | 7 | Nearby Objects fetcher. Coordinates JPL SSD and CNEOS near-Earth object data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `OpenScience` | 8 | Open Science fetcher. Coordinates NASA Open Science Data Repository resources within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `SpaceWeather` | 3 | Space Weather fetcher. Coordinates NASA DONKI space weather endpoints within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `AstroCatalog` | 6 | Astro Catalog fetcher. Coordinates Open Astronomy Catalog queries within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `AstroQuery` | 5 | Astro Query fetcher. Coordinates Simbad and astronomy object search operations within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `StarMap` | 6 | Star Map fetcher. Coordinates astronomical object map links and imagery within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `GovData` | 8 | Gov Data fetcher. Coordinates Data.gov package and collection retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `StarChart` | 7 | Star Chart fetcher. Coordinates static star chart and coordinate chart generation within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `Congress` | 16 | Congress fetcher. Coordinates Congress.gov legislative data retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `InternetArchive` | 5 | Internet Archive fetcher. Coordinates Internet Archive search and metadata retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `OpenWeather` | 6 | Open Weather fetcher. Coordinates Open-Meteo current and forecast weather retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance. |
| `HistoricalWeather` | 4 | Historical Weather fetcher. Coordinates historical weather archive retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance. |
| `Grokipedia` | 4 | Grokipedia fetcher. Coordinates Grokipedia search and page retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `GoogleGeocoding` | 6 | Google Geocoding fetcher. Coordinates Google forward, reverse, and place geocoding within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `CensusData` | 7 | Census Data fetcher. Coordinates U.S. Census dataset and variable retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `Socrata` | 8 | Socrata fetcher. Coordinates Socrata dataset metadata and row retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `HealthData` | 8 | Health Data fetcher. Coordinates HealthData.gov Socrata metadata and rows within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `GlobalHealthData` | 5 | Global Health Data fetcher. Coordinates WHO global health indicator and Athena data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `UnitedNations` | 5 | United Nations fetcher. Coordinates United Nations SDMX dataset and query retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `WorldPopulation` | 8 | World Population fetcher. Coordinates WorldPop catalog and raster metadata retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `Wonder` | 6 | Wonder fetcher. Coordinates CDC WONDER template and query submission within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `USGSEarthquakes` | 16 | U S G S Earthquakes fetcher. Coordinates USGS earthquake feed and query retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `USGSWaterData` | 16 | U S G S Water Data fetcher. Coordinates USGS water services records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance. |
| `USGSTheNationalMap` | 14 | U S G S The National Map fetcher. Coordinates USGS National Map datasets and products within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `USGSScienceBase` | 13 | U S G S Science Base fetcher. Coordinates USGS ScienceBase items and catalog records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `AirNow` | 10 | Air Now fetcher. Coordinates AirNow current and forecast air quality data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `ClimateData` | 13 | Climate Data fetcher. Coordinates NOAA climate dataset and data records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `EoNet` | 15 | Eo Net fetcher. Coordinates NASA EONET environmental event data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `EnviroFacts` | 11 | Enviro Facts fetcher. Coordinates EPA Envirofacts table and facility records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `TidesAndCurrents` | 17 | Tides And Currents fetcher. Coordinates NOAA tides, currents, and station data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `UvIndex` | 14 | UV Index fetcher. Coordinates EPA UV Index current and forecast data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `PurpleAir` | 19 | Purple Air fetcher. Coordinates PurpleAir sensor and air quality records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `OpenAQ` | 22 | Open A Q fetcher. Coordinates OpenAQ location, measurement, and air-quality records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `Firms` | 15 | Firms fetcher. Coordinates NASA FIRMS active fire data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |
| `OpenSky` | 22 | Open Sky fetcher. Coordinates OpenSky Network aircraft, airport, and state-vector data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. |

## `Fetcher`

Fetcher fetcher. Coordinates base fetcher operations within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch()` | `fetch( query: str, url: str, time: int = 10 ) -> Result \| None` | Fetch base fetcher operations. Retrieves base fetcher operations using validated arguments and the stored Fetcher runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Result \| None: Normalized provider response, result payload, or document collection |

## `WebFetcher`

Web Fetcher fetcher. Coordinates HTTP web page retrieval and HTML extraction within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch()` | `fetch( url: str, time: int = 10 ) -> Result \| None` | Fetch HTTP web page retrieval and HTML extraction. Retrieves HTTP web page retrieval and HTML extraction using validated arguments and the stored WebFetcher runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `html_to_text()` | `html_to_text( html: str ) -> str` | HTML to text. Converts raw HTML into compact plain text by removing scripts, styles, markup, and repeated whitespace. The method prepares fetched web content for search, summarization, display, or downstream text processing. str: Plain-text representation of the supplied HTML content. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `coerce_items()` | `coerce_items( value: Any ) -> List[str]` | Coerce items. Coerces items into a predictable Python representation for downstream handling. The method prefers structured data when available and falls back to text or simple containers when the provider response cannot be represented more specifically. List[str]: Result produced by the operation. |
| `extract_title()` | `extract_title( html: str ) -> str` | Extract title. Extracts title from the supplied source content and normalizes the result for downstream use. The method stores intermediate parsing state when needed and returns only the compact value required by callers. str: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `truncate_text()` | `truncate_text( text: str, limit: int = 12000 ) -> str` | Truncate text. Performs the truncate text operation for HTTP web page retrieval and HTML extraction. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `normalize_url()` | `normalize_url( base_url: str, href: str ) -> str` | Normalize url. Converts url into the canonical format expected by the WebFetcher request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. |
| `same_domain()` | `same_domain( left_url: str, right_url: str ) -> bool` | Same domain. Performs the same domain operation for HTTP web page retrieval and HTML extraction. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. bool: Result produced by the operation. |
| `extract_links()` | `extract_links( base_url: str, html: str ) -> List[str]` | Extract links. Extracts links from the supplied source content and normalizes the result for downstream use. The method stores intermediate parsing state when needed and returns only the compact value required by callers. List[str]: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `extract_structured_data()` | `extract_structured_data( url: str, html: str, selected_methods: Optional[List[str]] = None ) -> Dict[str, List[str]]` | Extract structured data. Extracts structured data from the supplied source content and normalizes the result for downstream use. The method stores intermediate parsing state when needed and returns only the compact value required by callers. structured data operation. Dict[str, List[str]]: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_paragraphs()` | `scrape_paragraphs( uri: str ) -> List[str] \| None` | Scrape paragraphs. Scrapes paragraphs from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_lists()` | `scrape_lists( uri: str ) -> List[str] \| None` | Scrape lists. Scrapes lists from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_tables()` | `scrape_tables( uri: str ) -> List[str] \| None` | Scrape tables. Scrapes tables from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_articles()` | `scrape_articles( uri: str ) -> List[str] \| None` | Scrape articles. Scrapes articles from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_headings()` | `scrape_headings( uri: str ) -> List[str] \| None` | Scrape headings. Scrapes headings from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_divisions()` | `scrape_divisions( uri: str ) -> List[str] \| None` | Scrape divisions. Scrapes divisions from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_sections()` | `scrape_sections( uri: str ) -> List[str] \| None` | Scrape sections. Scrapes sections from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_blockquotes()` | `scrape_blockquotes( uri: str ) -> List[str] \| None` | Scrape blockquotes. Scrapes blockquotes from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_hyperlinks()` | `scrape_hyperlinks( uri: str ) -> List[str] \| None` | Scrape hyperlinks. Scrapes hyperlinks from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_images()` | `scrape_images( uri: str ) -> List[str] \| None` | Scrape images. Scrapes images from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. List[str] \| None: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `WebCrawler`

Web Crawler fetcher. Coordinates bounded web crawling and optional browser-rendered scraping within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch()` | `fetch( url: str, time: int = 10 ) -> Result \| None` | Fetch bounded web crawling and optional browser-rendered scraping. Retrieves bounded web crawling and optional browser-rendered scraping using validated arguments and the stored WebCrawler runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `render_with_playwright()` | `render_with_playwright( url: str, timeout: int = 15 ) -> str` | Render with playwright. Performs the render with playwright operation for bounded web crawling and optional browser-rendered scraping. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `scrape_page()` | `scrape_page( url: str, include_title: bool = True, include_basic_text: bool = True, include_raw_html: bool = False, selected_methods: Optional[List[str]] = None, request_timeout: int = 10, max_bytes: int = 1000000 ) -> Dict[str, Any]` | Scrape page. Scrapes page from a fetched HTML document through the configured web retrieval path. The method validates the requested URI, obtains page content, delegates structured extraction, and returns normalized text or link values. operation. Dict[str, Any]: Extracted and normalized content values. |
| `crawl()` | `crawl( seed_url: str, include_title: bool = True, include_basic_text: bool = True, include_raw_html: bool = False, selected_methods: Optional[List[str]] = None, recursive: bool = False, max_depth: int = 1, max_pages: int = 10, same_domain_only: bool = True, request_timeout: int = 10, delay_seconds: float = 0.25, max_bytes: int = 1000000 ) -> Dict[str, Any]` | Crawl. Performs the crawl operation for bounded web crawling and optional browser-rendered scraping. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. operation. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |

## `ArXiv`

Ar Xiv fetcher. Coordinates ArXiv research document retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch()` | `fetch( question: str, max_documents: int = None, full_documents: bool = None, include_metadata: bool = None ) -> List[Document] \| None` | Fetch ArXiv research document retrieval. Retrieves ArXiv research documents using validated arguments and the stored ArXiv runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. List[Document] \| None: Normalized provider response, result payload, or document |

## `GoogleDrive`

Google Drive fetcher. Coordinates Google Drive document retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance.

| Method | Signature | Purpose |
|---|---|---|
| `mime_options()` | `mime_options(  ) -> List[str]` | Mime options. Performs the mime options operation for Google Drive document retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[str]: Result produced by the operation. |
| `template_options()` | `template_options(  ) -> List[str]` | Template options. Performs the template options operation for Google Drive document retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[str]: Result produced by the operation. |
| `mode_options()` | `mode_options(  ) -> List[str]` | Mode options. Performs the mode options operation for Google Drive document retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[str]: Result produced by the operation. |
| `fetch()` | `fetch( question: str, folder_id: str = 'root', results: int = 10, template: str = 'gdrive-query', mime_type: str = None, mode: str = 'documents' ) -> List[Document] \| None` | Fetch Google Drive document retrieval. Retrieves Google Drive document retrieval using validated arguments and the stored GoogleDrive runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. List[Document] \| None: Normalized provider response, result payload, or document |

## `Wikipedia`

Wikipedia fetcher. Coordinates Wikipedia document retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch()` | `fetch( question: str, language: str = None, max_documents: int = None, include_metadata: bool = None ) -> List[Document] \| None` | Fetch Wikipedia document retrieval. Retrieves Wikipedia document retrieval using validated arguments and the stored Wikipedia runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. List[Document] \| None: Normalized provider response, result payload, or document |

## `TheNews`

The News fetcher. Coordinates The News API article retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch()` | `fetch( endpoint: str = 'all', query: str = '', language: str = 'en', categories: str = '', exclude_categories: str = '', locale: str = '', domains: str = '', exclude_domains: str = '', source_ids: str = '', exclude_source_ids: str = '', published_after: str = '', published_before: str = '', published_on: str = '', sort: str = 'published_at', limit: int = 10, page: int = 1, include_similar: bool = True, headlines_per_category: int = 6, time: int = 10, api_key: str = None ) -> Dict[str, Any]` | Fetch The News API article retrieval. Retrieves The News API article retrieval using validated arguments and the stored TheNews runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any]: Normalized provider response, result payload, or document collection |

## `GoogleSearch`

Google Search fetcher. Coordinates Google Custom Search retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch()` | `fetch( keywords: str, results: int = 10, start: int = 1, exact_terms: str = '', exclude_terms: str = '', file_type: str = '', date_restrict: str = '', gl: str = '', lr: str = '', safe: str = 'off', search_type: str = '', site_search: str = '', site_search_filter: str = '', sort: str = '', img_size: str = '', img_type: str = '', img_color_type: str = '', img_dominant_color: str = '', time: int = 10, api_key: str = None, cse_id: str = None ) -> Dict[str, Any] \| None` | Fetch Google Custom Search retrieval. Retrieves Google Custom Search retrieval using validated arguments and the stored GoogleSearch runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |

## `GoogleMaps`

Google Maps fetcher. Coordinates Google Maps geocoding, address validation, and directions within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `geocode_location()` | `geocode_location( address: str ) -> Tuple[float, float]` | Geocode location. Performs the geocode location operation for Google Maps geocoding, address validation, and directions. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Tuple[float, float]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `geocode_coordinates()` | `geocode_coordinates( lat: float, long: float ) -> str \| None` | Geocode coordinates. Performs the geocode coordinates operation for Google Maps geocoding, address validation, and directions. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_address()` | `validate_address( address: List[str] ) -> Dict[Any, Any] \| None` | Validate address. Validates and normalizes address before it is used in Google Maps geocoding, address validation, and directions. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. Dict[Any, Any] \| None: Validated address value. Error: Re-raised after the original exception is wrapped and written to the application |
| `request_directions()` | `request_directions( origin: str, destination: str, mode: str = 'driving' ) -> Dict[str, Any] \| None` | Request directions. Performs the request directions operation for Google Maps geocoding, address validation, and directions. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `GoogleWeather`

Google Weather fetcher. Coordinates Google Weather conditions, forecasts, history, and alerts within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `resolve_coordinates()` | `resolve_coordinates( address: str ) -> Tuple[float, float]` | Resolve coordinates. Performs the resolve coordinates operation for Google Weather conditions, forecasts, history, and alerts. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Tuple[float, float]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `request()` | `request( path: str, params: Dict[str, Any], time: int = 10 ) -> Dict[str, Any] \| None` | Request Google Weather conditions, forecasts, history, and alerts. Sends a provider request for Google Weather conditions, forecasts, history, and alerts after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `package_response()` | `package_response(  ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `fetch_current()` | `fetch_current( address: str, units_system: str = 'METRIC', language_code: str = 'en', time: int = 10 ) -> Dict[str, Any] \| None` | Fetch current. Retrieves current using validated arguments and the stored GoogleWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_hourly_forecast()` | `fetch_hourly_forecast( address: str, hours: int = 24, units_system: str = 'METRIC', language_code: str = 'en', time: int = 10 ) -> Dict[str, Any] \| None` | Fetch hourly forecast. Retrieves hourly forecast using validated arguments and the stored GoogleWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_daily_forecast()` | `fetch_daily_forecast( address: str, days: int = 5, units_system: str = 'METRIC', language_code: str = 'en', time: int = 10 ) -> Dict[str, Any] \| None` | Fetch daily forecast. Retrieves daily forecast using validated arguments and the stored GoogleWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_hourly_history()` | `fetch_hourly_history( address: str, hours: int = 24, units_system: str = 'METRIC', language_code: str = 'en', time: int = 10 ) -> Dict[str, Any] \| None` | Fetch hourly history. Retrieves hourly history using validated arguments and the stored GoogleWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_alerts()` | `fetch_alerts( address: str, language_code: str = 'en', time: int = 10 ) -> Dict[str, Any] \| None` | Fetch alerts. Retrieves alerts using validated arguments and the stored GoogleWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |

## `NavalObservatory`

Naval Observatory fetcher. Coordinates U.S. Naval Observatory celestial-navigation data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_date()` | `validate_date( date_value: str ) -> str` | Validate date. Validates and normalizes date before it is used in U.S. Naval Observatory celestial-navigation data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated date value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_time()` | `validate_time( time_value: str ) -> str` | Validate time. Validates and normalizes time before it is used in U.S. Naval Observatory celestial-navigation data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated time value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_coordinates()` | `validate_coordinates( latitude: float, longitude: float ) -> tuple[float, float]` | Validate coordinates. Validates and normalizes coordinates before it is used in U.S. Naval Observatory celestial-navigation data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. tuple[float, float]: Validated coordinates value. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_celnav()` | `fetch_celnav( date_value: str, time_value: str, latitude: float, longitude: float, location_label: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch celnav. Retrieves celnav using validated arguments and the stored NavalObservatory runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'celnav', date_value: str = '', time_value: str = '', latitude: float = 0.0, longitude: float = 0.0, location_label: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch U.S. Naval Observatory celestial-navigation data. Retrieves U.S. Naval Observatory celestial-navigation data using validated arguments and the stored NavalObservatory runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `SatelliteCenter`

Satellite Center fetcher. Coordinates SSC satellite observatory, ground-station, and location data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance. instance.

| Method | Signature | Purpose |
|---|---|---|
| `fetch_observatories()` | `fetch_observatories(  ) -> Dict[str, Any] \| None` | Fetch observatories. Retrieves observatories using validated arguments and the stored SatelliteCenter runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_ground_stations()` | `fetch_ground_stations(  ) -> Dict[str, Any] \| None` | Fetch ground stations. Retrieves ground stations using validated arguments and the stored SatelliteCenter runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_locations()` | `fetch_locations( observatories: str, start_time: str, end_time: str, coordinate_systems: str = 'gse', resolution_factor: int = 1, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch locations. Retrieves locations using validated arguments and the stored SatelliteCenter runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'observatories', query: str = '', start_time: str = '', end_time: str = '', coordinate_systems: str = 'gse', resolution_factor: int = 1, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch SSC satellite observatory, ground-station, and location data. Retrieves SSC satellite observatory, ground-station, and location data using validated arguments and the stored SatelliteCenter runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream |

## `EarthObservatory`

Earth Observatory fetcher. Coordinates NASA EONET events, categories, sources, and layers within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch_events()` | `fetch_events( status: str = 'open', category: str = '', source: str = '', limit: int = 20, days: int = 30, start_date: str = '', end_date: str = '', time: int = 20 ) -> Dict[str, Any]` | Fetch events. Retrieves events using validated arguments and the stored EarthObservatory runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any]: Normalized provider response, result payload, or document collection |
| `fetch_categories()` | `fetch_categories( time: int = 20 ) -> Dict[str, Any] \| None` | Fetch categories. Retrieves categories using validated arguments and the stored EarthObservatory runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_sources()` | `fetch_sources( time: int = 20 ) -> Dict[str, Any] \| None` | Fetch sources. Retrieves sources using validated arguments and the stored EarthObservatory runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_layers()` | `fetch_layers( category: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch layers. Retrieves layers using validated arguments and the stored EarthObservatory runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'events', status: str = 'open', category: str = '', source: str = '', limit: int = 20, days: int = 30, start_date: str = '', end_date: str = '', time: int = 20 ) -> Dict[str, Any]` | Fetch NASA EONET events, categories, sources, and layers. Retrieves NASA EONET events, categories, sources, and layers using validated arguments and the stored EarthObservatory runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `GlobalImagery`

Global Imagery fetcher. Coordinates NASA GIBS WMS imagery and map service retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `get_capabilities_url()` | `get_capabilities_url( projection: str = 'epsg4326', quality: str = 'best', version: str = '1.1.1' ) -> str` | Get capabilities url. Performs the get capabilities url operation for NASA GIBS WMS imagery and map service retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `build_wms_url()` | `build_wms_url( layer: str, image_date: str, bbox: Tuple[float, float, float, float], width: int = 1200, height: int = 600, projection: str = 'epsg4326', quality: str = 'best', image_format: str = 'image/png', transparent: bool = True, version: str = '1.1.1' ) -> str` | Build wms url. Constructs wms url from validated inputs and stored runtime configuration. The method centralizes URL, parameter, or payload assembly so fetch operations can issue provider requests using a predictable structure. operation. str: Constructed URL, query, payload, or request structure. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_wms_map()` | `fetch_wms_map( layer: str, image_date: str, bbox: Tuple[float, float, float, float], width: int = 1200, height: int = 600, projection: str = 'epsg4326', quality: str = 'best', image_format: str = 'image/png', transparent: bool = True, output_dir: str = 'python-examples', output_name: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch wms map. Retrieves wms map using validated arguments and the stored GlobalImagery runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_map_services()` | `fetch_map_services(  ) -> Dict[str, Any] \| None` | Fetch map services. Retrieves map services using validated arguments and the stored GlobalImagery runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_mercator_map()` | `fetch_mercator_map( ccrs: Any = None ) -> Dict[str, Any] \| None` | Fetch mercator map. Retrieves mercator map using validated arguments and the stored GlobalImagery runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `NearbyObjects`

Nearby Objects fetcher. Coordinates JPL SSD and CNEOS near-Earth object data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch_close_approaches()` | `fetch_close_approaches( start_date: str, end_date: str, dist_max: str = '10LD', body: str = 'Earth', sort: str = 'date', limit: int = 20, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch close approaches. Retrieves close approaches using validated arguments and the stored NearbyObjects runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_object_lookup()` | `fetch_object_lookup( query: str, query_type: str = 'sstr', include_physical: bool = True, include_close_approaches: bool = True, ca_body: str = 'Earth', include_discovery: bool = True, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch object lookup. Retrieves object lookup using validated arguments and the stored NearbyObjects runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. operation. lookup operation. operation. |
| `fetch_nhats_summary()` | `fetch_nhats_summary( dv: float = 6.0, dur: int = 360, stay: int = 8, launch: str = '2020-2045', h: float = 26.0, occ: int = 7, time: int = 20 ) -> Dict[str, Any]` | Fetch nhats summary. Retrieves nhats summary using validated arguments and the stored NearbyObjects runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_nhats_object()` | `fetch_nhats_object( designation: str, dv: float = 6.0, dur: int = 360, stay: int = 8, launch: str = '2020-2045', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch nhats object. Retrieves nhats object using validated arguments and the stored NearbyObjects runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_fireballs()` | `fetch_fireballs( date_min: str = '', limit: int = 20, time: int = 20 ) -> Dict[str, Any]` | Fetch fireballs. Retrieves fireballs using validated arguments and the stored NearbyObjects runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'close_approaches', start_date: str = '', end_date: str = '', query: str = '', query_type: str = 'sstr', dist_max: str = '10LD', body: str = 'Earth', sort: str = 'date', limit: int = 20, dv: float = 6.0, dur: int = 360, stay: int = 8, launch: str = '2020-2045', h: float = 26.0, occ: int = 7, include_physical: bool = True, include_close_approaches: bool = True, ca_body: str = 'Earth', include_discovery: bool = True, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch JPL SSD and CNEOS near-Earth object data. Retrieves JPL SSD and CNEOS near-Earth object data using validated arguments and the stored NearbyObjects runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `OpenScience`

Open Science fetcher. Coordinates NASA Open Science Data Repository resources within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_format()` | `validate_format( format_value: str ) -> str` | Validate format. Validates and normalizes format before it is used in NASA Open Science Data Repository resources. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated format value. Error: Re-raised after the original exception is wrapped and written to the application |
| `coerce_response()` | `coerce_response( response: requests.Response ) -> Dict[str, Any] \| str` | Coerce response. Coerces response into a predictable Python representation for downstream handling. The method prefers structured data when available and falls back to text or simple containers when the provider response cannot be represented more specifically. Dict[str, Any] \| str: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_dataset()` | `fetch_dataset( accession: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch dataset. Retrieves dataset using validated arguments and the stored OpenScience runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_metadata()` | `fetch_metadata( query: str, format_value: str = 'json', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch metadata. Retrieves metadata using validated arguments and the stored OpenScience runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_assays()` | `fetch_assays( query: str, format_value: str = 'json', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch assays. Retrieves assays using validated arguments and the stored OpenScience runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_data()` | `fetch_data( query: str, format_value: str = 'json', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch data. Retrieves data using validated arguments and the stored OpenScience runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'dataset', query: str = '', accession: str = '', format_value: str = 'json', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch NASA Open Science Data Repository resources. Retrieves NASA Open Science Data Repository resources using validated arguments and the stored OpenScience runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `SpaceWeather`

Space Weather fetcher. Coordinates NASA DONKI space weather endpoints within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch_endpoint()` | `fetch_endpoint( endpoint: str, start_date: str, end_date: str, time: int = 20, location: str = '', catalog: str = '', notification_type: str = '', most_accurate_only: bool = True, complete_entry_only: bool = True, speed: int = 0, half_angle: int = 0, keyword: str = '', api_key: str = None ) -> Dict[str, Any] \| None` | Fetch endpoint. Retrieves endpoint using validated arguments and the stored SpaceWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. operation. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'cme', start_date: str = '', end_date: str = '', time: int = 20, location: str = 'ALL', catalog: str = 'ALL', notification_type: str = 'all', most_accurate_only: bool = True, complete_entry_only: bool = True, speed: int = 0, half_angle: int = 0, keyword: str = '', api_key: str = None ) -> Dict[str, Any] \| None` | Fetch NASA DONKI space weather endpoints. Retrieves NASA DONKI space weather endpoints using validated arguments and the stored SpaceWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `AstroCatalog`

Astro Catalog fetcher. Coordinates Open Astronomy Catalog queries within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize_attribute_path()` | `normalize_attribute_path( quantity: str = '', attributes: str = '' ) -> str` | Normalize attribute path. Converts attribute path into the canonical format expected by the AstroCatalog request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. |
| `parse_argument()` | `parse_argument( argument_string: str ) -> Dict[str, Any]` | Parse argument. Performs the parse argument operation for Open Astronomy Catalog queries. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. |
| `request()` | `request( route: str, params: Dict[str, Any] \| None = None, time: int = 20 ) -> Any` | Request Open Astronomy Catalog queries. Sends a provider request for Open Astronomy Catalog queries after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. operation. object: Normalized provider response, result payload, or document collection returned by |
| `fetch_object()` | `fetch_object( name: str, quantity: str = '', attributes: str = '', arguments: str = '', data_format: str = 'json', time: int = 20 ) -> Any` | Fetch object. Retrieves object using validated arguments and the stored AstroCatalog runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. object: Normalized provider response, result payload, or document collection returned by |
| `cone_search()` | `cone_search( ra: str, dec: str, radius: int = 2, quantity: str = '', attributes: str = '', arguments: str = '', data_format: str = 'json', time: int = 20 ) -> Any` | Cone search. Performs the cone search operation for Open Astronomy Catalog queries. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. object: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `fetch()` | `fetch( mode: str = 'object_query', query: str = '', quantity: str = '', attributes: str = '', arguments: str = '', ra: str = '', dec: str = '', radius: int = 2, data_format: str = 'json', time: int = 20 ) -> Any` | Fetch Open Astronomy Catalog queries. Retrieves Open Astronomy Catalog queries using validated arguments and the stored AstroCatalog runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. object: Normalized provider response, result payload, or document collection returned by |

## `AstroQuery`

Astro Query fetcher. Coordinates Simbad and astronomy object search operations within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `table_to_records()` | `table_to_records( table: Table \| None ) -> List[Dict[str, Any]]` | Table to records. Performs the table to records operation for Simbad and astronomy object search operations. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[Dict[str, Any]]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `object_search()` | `object_search( name: str, row_limit: int = 100 ) -> Dict[str, Any] \| None` | Object search. Performs the object search operation for Simbad and astronomy object search operations. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `object_ids()` | `object_ids( name: str, row_limit: int = 100 ) -> Dict[str, Any] \| None` | Object IDs. Performs the object ids operation for Simbad and astronomy object search operations. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `region_search()` | `region_search( ra: str, dec: str, radius: float = 0.5, radius_unit: str = 'deg', row_limit: int = 100 ) -> Dict[str, Any] \| None` | Region search. Performs the region search operation for Simbad and astronomy object search operations. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch()` | `fetch( mode: str = 'object_search', query: str = '', ra: str = '', dec: str = '', radius: float = 0.5, radius_unit: str = 'deg', row_limit: int = 100 ) -> Dict[str, Any]` | Fetch Simbad and astronomy object search operations. Retrieves Simbad and astronomy object search operations using validated arguments and the stored AstroQuery runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |

## `StarMap`

Star Map fetcher. Coordinates astronomical object map links and imagery within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize()` | `normalize( value: bool ) -> str` | Normalize. Performs the normalize operation for astronomical object map links and imagery. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str: Result produced by the operation. |
| `extract_links()` | `extract_links( html: str, base_url: str ) -> Dict[str, str]` | Extract links. Extracts links from the supplied source content and normalizes the result for downstream use. The method stores intermediate parsing state when needed and returns only the compact value required by callers. Dict[str, str]: Extracted and normalized content values. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `fetch_object_link()` | `fetch_object_link( name: str, zoom: int = 5, box_color: str = 'yellow', show_box: bool = True, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch object link. Retrieves object link using validated arguments and the stored StarMap runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_coordinate_link()` | `fetch_coordinate_link( ra: float, dec: float, zoom: int = 5, box_color: str = 'yellow', show_box: bool = True, show_grid: bool = True, show_lines: bool = True, show_boundaries: bool = True, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch coordinate link. Retrieves coordinate link using validated arguments and the stored StarMap runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_snapshot()` | `fetch_snapshot( ra: float, dec: float, zoom: int = 10, image_source: str = 'DSS2', show_grid: bool = True, show_lines: bool = True, show_boundaries: bool = True, show_const_names: bool = False, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch snapshot. Retrieves snapshot using validated arguments and the stored StarMap runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'object_link', query: str = '', ra: float = 0.0, dec: float = 0.0, zoom: int = 5, image_source: str = 'DSS2', box_color: str = 'yellow', show_box: bool = True, show_grid: bool = True, show_lines: bool = True, show_boundaries: bool = True, show_const_names: bool = False, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch astronomical object map links and imagery. Retrieves astronomical object map links and imagery using validated arguments and the stored StarMap runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |

## `GovData`

Gov Data fetcher. Coordinates Data.gov package and collection retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_page_size()` | `validate_page_size( page_size: int ) -> int` | Validate page size. Validates and normalizes page size before it is used in Data.gov package and collection retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated page size value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_sort_field()` | `validate_sort_field( sort_field: str ) -> str` | Validate sort field. Validates and normalizes sort field before it is used in Data.gov package and collection retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated sort field value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_sort_order()` | `validate_sort_order( sort_order: str ) -> str` | Validate sort order. Validates and normalizes sort order before it is used in Data.gov package and collection retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated sort order value. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_search()` | `fetch_search( query: str, page_size: int = 10, offset_mark: str = '*', sort_field: str = 'score', sort_order: str = 'DESC', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch search. Retrieves search using validated arguments and the stored GovData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_package_summary()` | `fetch_package_summary( package_id: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch package summary. Retrieves package summary using validated arguments and the stored GovData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_collection()` | `fetch_collection( collection: str, start_date: str, page_size: int = 10, offset_mark: str = '*', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch collection. Retrieves collection using validated arguments and the stored GovData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'search', query: str = '', page_size: int = 10, offset_mark: str = '*', sort_field: str = 'score', sort_order: str = 'DESC', package_id: str = '', collection: str = '', start_date: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch Data.gov package and collection retrieval. Retrieves Data.gov package and collection retrieval using validated arguments and the stored GovData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `StarChart`

Star Chart fetcher. Coordinates static star chart and coordinate chart generation within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `flag()` | `flag( value: bool, invert: bool = False ) -> int` | Flag. Performs the flag operation for static star chart and coordinate chart generation. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. int: Result produced by the operation. |
| `search_object()` | `search_object( name: str, time: int = 20 ) -> Dict[str, Any] \| None` | Search object. Performs the search object operation for static star chart and coordinate chart generation. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_object_chart()` | `fetch_object_chart( name: str, zoom: int = 5, box_color: str = 'yellow', show_box: bool = True, image_source: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch object chart. Retrieves object chart using validated arguments and the stored StarChart runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_coordinate_chart()` | `fetch_coordinate_chart( ra: float, dec: float, zoom: int = 5, box_color: str = 'yellow', show_box: bool = True, show_grid: bool = True, show_lines: bool = True, show_boundaries: bool = True, image_source: str = '' ) -> Dict[str, Any] \| None` | Fetch coordinate chart. Retrieves coordinate chart using validated arguments and the stored StarChart runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_static_chart()` | `fetch_static_chart( ra: float, dec: float, zoom: int = 5, image_source: str = 'DSS2', show_grid: bool = True, show_lines: bool = True, show_boundaries: bool = True, show_const_names: bool = False, width: int = 900, height: int = 450, magnitude: float = 7.5 ) -> Dict[str, Any] \| None` | Fetch static chart. Retrieves static chart using validated arguments and the stored StarChart runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'object_chart', query: str = '', ra: float = 0.0, dec: float = 0.0, zoom: int = 5, image_source: str = 'DSS2', box_color: str = 'yellow', show_box: bool = True, show_grid: bool = True, show_lines: bool = True, show_boundaries: bool = True, show_const_names: bool = False, width: int = 900, height: int = 450, magnitude: float = 7.5, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch static star chart and coordinate chart generation. Retrieves static star chart and coordinate chart generation using validated arguments and the stored StarChart runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `Congress`

Congress fetcher. Coordinates Congress.gov legislative data retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_limit()` | `validate_limit( limit: int ) -> int` | Validate limit. Validates and normalizes limit before it is used in Congress.gov legislative data retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated limit value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_offset()` | `validate_offset( offset: int ) -> int` | Validate offset. Validates and normalizes offset before it is used in Congress.gov legislative data retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated offset value. Error: Re-raised after the original exception is wrapped and written to the application |
| `normalize_bill_type()` | `normalize_bill_type( bill_type: str ) -> str` | Normalize bill type. Converts bill type into the canonical format expected by the Congress request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `normalize_law_type()` | `normalize_law_type( law_type: str ) -> str` | Normalize law type. Converts law type into the canonical format expected by the Congress request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `normalize_report_type()` | `normalize_report_type( report_type: str ) -> str` | Normalize report type. Converts report type into the canonical format expected by the Congress request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `build_params()` | `build_params( limit: int = 20, offset: int = 0, sort: str = 'updateDate+desc' ) -> Dict[str, Any]` | Build params. Constructs params from validated inputs and stored runtime configuration. The method centralizes URL, parameter, or payload assembly so fetch operations can issue provider requests using a predictable structure. Dict[str, Any]: Constructed URL, query, payload, or request structure. Error: Re-raised after the original exception is wrapped and written to the application |
| `request()` | `request( mode: str, url: str, params: Dict[str, Any], time: int = 20 ) -> Dict[str, Any] \| None` | Request Congress.gov legislative data retrieval. Sends a provider request for Congress.gov legislative data retrieval after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_congresses()` | `fetch_congresses( limit: int = 20, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch congresses. Retrieves congresses using validated arguments and the stored Congress runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_bills()` | `fetch_bills( congress: int, bill_type: str = '', offset: int = 0, limit: int = 20, sort: str = 'updateDate+desc', from_date_time: str = '', to_date_time: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch bills. Retrieves bills using validated arguments and the stored Congress runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_bill()` | `fetch_bill( congress: int, bill_type: str, bill_number: int, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch bill. Retrieves bill using validated arguments and the stored Congress runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_laws()` | `fetch_laws( congress: int, law_type: str = '', offset: int = 0, limit: int = 20, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch laws. Retrieves laws using validated arguments and the stored Congress runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_law()` | `fetch_law( congress: int, law_type: str, law_number: int, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch law. Retrieves law using validated arguments and the stored Congress runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_reports()` | `fetch_reports( congress: int, report_type: str = '', offset: int = 0, limit: int = 20, conference: bool = False, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch reports. Retrieves reports using validated arguments and the stored Congress runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_report()` | `fetch_report( congress: int, report_type: str, report_number: int, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch report. Retrieves report using validated arguments and the stored Congress runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'congresses', congress: int = 0, bill_type: str = '', bill_number: int = 0, law_type: str = '', law_number: int = 0, report_type: str = '', report_number: int = 0, offset: int = 0, limit: int = 20, sort: str = 'updateDate+desc', from_date_time: str = '', to_date_time: str = '', conference: bool = False, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch Congress.gov legislative data retrieval. Retrieves Congress.gov legislative data retrieval using validated arguments and the stored Congress runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `InternetArchive`

Internet Archive fetcher. Coordinates Internet Archive search and metadata retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_rows()` | `validate_rows( rows: int ) -> int` | Validate rows. Validates and normalizes rows before it is used in Internet Archive search and metadata retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated rows value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_page()` | `validate_page( page: int ) -> int` | Validate page. Validates and normalizes page before it is used in Internet Archive search and metadata retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated page value. Error: Re-raised after the original exception is wrapped and written to the application |
| `build_query()` | `build_query( keywords: str, media_type: str = '', collection: str = '' ) -> str` | Build query. Constructs query from validated inputs and stored runtime configuration. The method centralizes URL, parameter, or payload assembly so fetch operations can issue provider requests using a predictable structure. str: Constructed URL, query, payload, or request structure. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `fetch()` | `fetch( keywords: str, fields: List[str] \| None = None, rows: int = 10, page: int = 1, sort: str = 'downloads desc', media_type: str = '', collection: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch Internet Archive search and metadata retrieval. Retrieves Internet Archive search and metadata retrieval using validated arguments and the stored InternetArchive runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `OpenWeather`

Open Weather fetcher. Coordinates Open-Meteo current and forecast weather retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance.

| Method | Signature | Purpose |
|---|---|---|
| `geocode_location()` | `geocode_location( location: str, count: int = 10 ) -> Dict[str, Any] \| None` | Geocode location. Performs the geocode location operation for Open-Meteo current and forecast weather retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_current()` | `fetch_current( lat: float, long: float, zone: str = 'auto', past_days: int = 0 ) -> Dict[str, Any] \| None` | Fetch current. Retrieves current using validated arguments and the stored OpenWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_hourly()` | `fetch_hourly( lat: float, long: float, zone: str = 'auto', forecast_days: int = 7, past_days: int = 0 ) -> Dict[str, Any] \| None` | Fetch hourly. Retrieves hourly using validated arguments and the stored OpenWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_daily()` | `fetch_daily( lat: float, long: float, zone: str = 'auto', forecast_days: int = 7, past_days: int = 0 ) -> Dict[str, Any] \| None` | Fetch daily. Retrieves daily using validated arguments and the stored OpenWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( location: str, mode: str = 'current', zone: str = 'auto', forecast_days: int = 7, past_days: int = 0, count: int = 10 ) -> Dict[str, Any] \| None` | Fetch Open-Meteo current and forecast weather retrieval. Retrieves Open-Meteo current and forecast weather retrieval using validated arguments and the stored OpenWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `HistoricalWeather`

Historical Weather fetcher. Coordinates historical weather archive retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance.

| Method | Signature | Purpose |
|---|---|---|
| `geocode_location()` | `geocode_location( location: str, count: int = 10 ) -> Dict[str, Any] \| None` | Geocode location. Performs the geocode location operation for historical weather archive retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_historical()` | `fetch_historical( lat: float, long: float, date: dt.date, zone: str = 'auto' ) -> Dict[str, Any] \| None` | Fetch historical. Retrieves historical using validated arguments and the stored HistoricalWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( location: str, date: dt.date, zone: str = 'auto', count: int = 10 ) -> Dict[str, Any] \| None` | Fetch historical weather archive retrieval. Retrieves historical weather archive retrieval using validated arguments and the stored HistoricalWeather runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `Grokipedia`

Grokipedia fetcher. Coordinates Grokipedia search and page retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `fetch_search()` | `fetch_search( query: str, limit: int = 12, offset: int = 0 ) -> Dict[str, Any] \| None` | Fetch search. Retrieves search using validated arguments and the stored Grokipedia runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_page()` | `fetch_page( page: str, include_content: bool = True ) -> Dict[str, Any] \| None` | Fetch page. Retrieves page using validated arguments and the stored Grokipedia runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'search', query: str = '', page: str = '', limit: int = 12, offset: int = 0, include_content: bool = True ) -> Dict[str, Any] \| None` | Fetch Grokipedia search and page retrieval. Retrieves Grokipedia search and page retrieval using validated arguments and the stored Grokipedia runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `GoogleGeocoding`

Google Geocoding fetcher. Coordinates Google forward, reverse, and place geocoding within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `request()` | `request( params: Dict[str, Any], time: int = 10, api_key: Optional[str] = None ) -> Dict[str, Any] \| None` | Request Google forward, reverse, and place geocoding. Sends a provider request for Google forward, reverse, and place geocoding after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_forward()` | `fetch_forward( query: str, language: str = 'en', region: str = '', time: int = 10, api_key: Optional[str] = None ) -> Dict[str, Any] \| None` | Fetch forward. Retrieves forward using validated arguments and the stored GoogleGeocoding runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_reverse()` | `fetch_reverse( latitude: float, longitude: float, language: str = 'en', result_type: str = '', location_type: str = '', time: int = 10, api_key: Optional[str] = None ) -> Dict[str, Any] \| None` | Fetch reverse. Retrieves reverse using validated arguments and the stored GoogleGeocoding runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_place()` | `fetch_place( place_id: str, language: str = 'en', region: str = '', time: int = 10, api_key: Optional[str] = None ) -> Dict[str, Any] \| None` | Fetch place. Retrieves place using validated arguments and the stored GoogleGeocoding runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'forward', query: str = '', latitude: float = 0.0, longitude: float = 0.0, place_id: str = '', language: str = 'en', region: str = '', result_type: str = '', location_type: str = '', time: int = 10, api_key: Optional[str] = None ) -> Dict[str, Any] \| None` | Fetch Google forward, reverse, and place geocoding. Retrieves Google forward, reverse, and place geocoding using validated arguments and the stored GoogleGeocoding runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `CensusData`

Census Data fetcher. Coordinates U.S. Census dataset and variable retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize_fields()` | `normalize_fields( fields: str ) -> str` | Normalize fields. Converts fields into the canonical format expected by the CensusData request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `parse_predicates()` | `parse_predicates( predicates: str = '' ) -> Dict[str, Any]` | Parse predicates. Performs the parse predicates operation for U.S. Census dataset and variable retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_table()` | `shape_table( rows: List[Any] ) -> Dict[str, Any]` | Shape table. Transforms raw provider records into table structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. Dict[str, Any]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_variables()` | `fetch_variables( year: str, dataset: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch variables. Retrieves variables using validated arguments and the stored CensusData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_data()` | `fetch_data( year: str, dataset: str, fields: str, geography_for: str = '', geography_in: str = '', predicates: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch data. Retrieves data using validated arguments and the stored CensusData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'variables', year: str = '2022', dataset: str = 'acs/acs5', fields: str = 'NAME,B01001_001E', geography_for: str = 'state:*', geography_in: str = '', predicates: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch U.S. Census dataset and variable retrieval. Retrieves U.S. Census dataset and variable retrieval using validated arguments and the stored CensusData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `Socrata`

Socrata fetcher. Coordinates Socrata dataset metadata and row retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize_domain()` | `normalize_domain( domain: str ) -> str` | Normalize domain. Converts domain into the canonical format expected by the Socrata request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `normalize_dataset_id()` | `normalize_dataset_id( dataset_id: str ) -> str` | Normalize dataset id. Converts dataset id into the canonical format expected by the Socrata request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_limit()` | `validate_limit( limit: int ) -> int` | Validate limit. Validates and normalizes limit before it is used in Socrata dataset metadata and row retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated limit value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_offset()` | `validate_offset( offset: int ) -> int` | Validate offset. Validates and normalizes offset before it is used in Socrata dataset metadata and row retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated offset value. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_metadata()` | `fetch_metadata( domain: str, dataset_id: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch metadata. Retrieves metadata using validated arguments and the stored Socrata runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_rows()` | `fetch_rows( domain: str, dataset_id: str, select: str = '', where: str = '', order: str = '', group: str = '', limit: int = 25, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch rows. Retrieves rows using validated arguments and the stored Socrata runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'rows', domain: str = 'data.cdc.gov', dataset_id: str = '', select: str = '', where: str = '', order: str = '', group: str = '', limit: int = 25, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch Socrata dataset metadata and row retrieval. Retrieves Socrata dataset metadata and row retrieval using validated arguments and the stored Socrata runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `HealthData`

Health Data fetcher. Coordinates HealthData.gov Socrata metadata and rows within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize_domain()` | `normalize_domain( domain: str ) -> str` | Normalize domain. Converts domain into the canonical format expected by the HealthData request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `normalize_dataset_id()` | `normalize_dataset_id( dataset_id: str ) -> str` | Normalize dataset id. Converts dataset id into the canonical format expected by the HealthData request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_limit()` | `validate_limit( limit: int ) -> int` | Validate limit. Validates and normalizes limit before it is used in HealthData.gov Socrata metadata and rows. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated limit value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_offset()` | `validate_offset( offset: int ) -> int` | Validate offset. Validates and normalizes offset before it is used in HealthData.gov Socrata metadata and rows. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated offset value. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_metadata()` | `fetch_metadata( domain: str, dataset_id: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch metadata. Retrieves metadata using validated arguments and the stored HealthData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_rows()` | `fetch_rows( domain: str, dataset_id: str, select: str = '', where: str = '', order: str = '', group: str = '', limit: int = 25, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch rows. Retrieves rows using validated arguments and the stored HealthData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'rows', domain: str = 'healthdata.gov', dataset_id: str = '', select: str = '', where: str = '', order: str = '', group: str = '', limit: int = 25, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch HealthData.gov Socrata metadata and rows. Retrieves HealthData.gov Socrata metadata and rows using validated arguments and the stored HealthData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `GlobalHealthData`

Global Health Data fetcher. Coordinates WHO global health indicator and Athena data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize_query_path()` | `normalize_query_path( query_path: str ) -> str` | Normalize query path. Converts query path into the canonical format expected by the GlobalHealthData request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_indicator_registry()` | `fetch_indicator_registry( time: int = 20 ) -> Dict[str, Any] \| None` | Fetch indicator registry. Retrieves indicator registry using validated arguments and the stored GlobalHealthData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_athena()` | `fetch_athena( query_path: str, fmt: str = 'json', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch athena. Retrieves athena using validated arguments and the stored GlobalHealthData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'indicator_registry', query_path: str = '', fmt: str = 'json', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch WHO global health indicator and Athena data. Retrieves WHO global health indicator and Athena data using validated arguments and the stored GlobalHealthData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `UnitedNations`

United Nations fetcher. Coordinates United Nations SDMX dataset and query retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize_query_path()` | `normalize_query_path( query_path: str ) -> str` | Normalize query path. Converts query path into the canonical format expected by the UnitedNations request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_datasets()` | `fetch_datasets( time: int = 20 ) -> Dict[str, Any] \| None` | Fetch datasets. Retrieves datasets using validated arguments and the stored UnitedNations runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_sdmx_query()` | `fetch_sdmx_query( query_path: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch sdmx query. Retrieves sdmx query using validated arguments and the stored UnitedNations runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'datasets', query_path: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch United Nations SDMX dataset and query retrieval. Retrieves United Nations SDMX dataset and query retrieval using validated arguments and the stored UnitedNations runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `WorldPopulation`

World Population fetcher. Coordinates WorldPop catalog and raster metadata retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize_asset_path()` | `normalize_asset_path( asset_path: str ) -> str` | Normalize asset path. Converts asset path into the canonical format expected by the WorldPopulation request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_page()` | `validate_page( page: int ) -> int` | Validate page. Validates and normalizes page before it is used in WorldPop catalog and raster metadata retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated page value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_page_size()` | `validate_page_size( page_size: int ) -> int` | Validate page size. Validates and normalizes page size before it is used in WorldPop catalog and raster metadata retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated page size value. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_catalog()` | `fetch_catalog( time: int = 20 ) -> Dict[str, Any] \| None` | Fetch catalog. Retrieves catalog using validated arguments and the stored WorldPopulation runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `search_catalog()` | `search_catalog( query: str = '', page: int = 1, page_size: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Search catalog. Performs the search catalog operation for WorldPop catalog and raster metadata retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_raster_metadata()` | `fetch_raster_metadata( asset_path: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch raster metadata. Retrieves raster metadata using validated arguments and the stored WorldPopulation runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'catalog', query: str = '', asset_path: str = '', page: int = 1, page_size: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch WorldPop catalog and raster metadata retrieval. Retrieves WorldPop catalog and raster metadata retrieval using validated arguments and the stored WorldPopulation runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `Wonder`

Wonder fetcher. Coordinates CDC WONDER template and query submission within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `normalize_dataset_id()` | `normalize_dataset_id( dataset_id: str ) -> str` | Normalize dataset id. Converts dataset id into the canonical format expected by the Wonder request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `build_template()` | `build_template( dataset_id: str = 'D76' ) -> str` | Build template. Constructs template from validated inputs and stored runtime configuration. The method centralizes URL, parameter, or payload assembly so fetch operations can issue provider requests using a predictable structure. str: Constructed URL, query, payload, or request structure. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `fetch_template()` | `fetch_template( dataset_id: str = 'D76' ) -> Dict[str, Any] \| None` | Fetch template. Retrieves template using validated arguments and the stored Wonder runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `submit_query()` | `submit_query( dataset_id: str, request_xml: str, time: int = 20 ) -> Dict[str, Any] \| None` | Submit query. Performs the submit query operation for CDC WONDER template and query submission. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch()` | `fetch( mode: str = 'metadata_template', dataset_id: str = 'D76', request_xml: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch CDC WONDER template and query submission. Retrieves CDC WONDER template and query submission using validated arguments and the stored Wonder runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `USGSEarthquakes`

U S G S Earthquakes fetcher. Coordinates USGS earthquake feed and query retrieval within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_feed()` | `validate_feed( feed: str ) -> str` | Validate feed. Validates and normalizes feed before it is used in USGS earthquake feed and query retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated feed value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_limit()` | `validate_limit( limit: int ) -> int` | Validate limit. Validates and normalizes limit before it is used in USGS earthquake feed and query retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated limit value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_magnitude()` | `validate_magnitude( name: str, value: float ) -> float` | Validate magnitude. Validates and normalizes magnitude before it is used in USGS earthquake feed and query retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. float: Validated magnitude value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_order_by()` | `validate_order_by( order_by: str ) -> str` | Validate order by. Validates and normalizes order by before it is used in USGS earthquake feed and query retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated order by value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_latitude()` | `validate_latitude( latitude: float ) -> float` | Validate latitude. Validates and normalizes latitude before it is used in USGS earthquake feed and query retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. float: Validated latitude value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_longitude()` | `validate_longitude( longitude: float ) -> float` | Validate longitude. Validates and normalizes longitude before it is used in USGS earthquake feed and query retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. float: Validated longitude value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_radius()` | `validate_radius( radius: float ) -> float` | Validate radius. Validates and normalizes radius before it is used in USGS earthquake feed and query retrieval. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. float: Validated radius value. Error: Re-raised after the original exception is wrapped and written to the application |
| `to_iso_date()` | `to_iso_date( value: str ) -> str` | Convert to iso date. Performs the to iso date operation for USGS earthquake feed and query retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `epoch_millis_to_iso()` | `epoch_millis_to_iso( value: Any ) -> str` | Epoch millis to iso. Performs the epoch millis to iso operation for USGS earthquake feed and query retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str: Result produced by the operation. |
| `shape_feature_rows()` | `shape_feature_rows( features: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape feature rows. Transforms raw provider records into feature rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. operation. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_features()` | `summarize_features( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize features. Performs the summarize features operation for USGS earthquake feed and query retrieval. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response(  ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `fetch_feed()` | `fetch_feed( feed: str = 'all_day.geojson', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch feed. Retrieves feed using validated arguments and the stored USGSEarthquakes runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_search()` | `fetch_search( start_date: str, end_date: str, min_magnitude: float = 1.0, max_magnitude: float = 10.0, limit: int = 25, order_by: str = 'time', event_type: str = 'earthquake', latitude: float \| None = None, longitude: float \| None = None, max_radius_km: float \| None = None, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch search. Retrieves search using validated arguments and the stored USGSEarthquakes runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'feed', feed: str = 'all_day.geojson', start_date: str = '', end_date: str = '', min_magnitude: float = 1.0, max_magnitude: float = 10.0, limit: int = 25, order_by: str = 'time', event_type: str = 'earthquake', latitude: float \| None = None, longitude: float \| None = None, max_radius_km: float \| None = None, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch USGS earthquake feed and query retrieval. Retrieves USGS earthquake feed and query retrieval using validated arguments and the stored USGSEarthquakes runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `USGSWaterData`

U S G S Water Data fetcher. Coordinates USGS water services records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface. instance.

| Method | Signature | Purpose |
|---|---|---|
| `validate_collection()` | `validate_collection( collection: str ) -> str` | Validate collection. Validates and normalizes collection before it is used in USGS water services records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated collection value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_limit()` | `validate_limit( limit: int ) -> int` | Validate limit. Validates and normalizes limit before it is used in USGS water services records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated limit value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_parameter_code()` | `validate_parameter_code( parameter_code: str ) -> str` | Validate parameter code. Validates and normalizes parameter code before it is used in USGS water services records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. operation. str: Validated parameter code value. Error: Re-raised after the original exception is wrapped and written to the application |
| `coalesce_records()` | `coalesce_records( payload: Any ) -> List[Dict[str, Any]]` | Coalesce records. Performs the coalesce records operation for USGS water services records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[Dict[str, Any]]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_monitoring_locations()` | `shape_monitoring_locations( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape monitoring locations. Transforms raw provider records into monitoring locations structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. operation. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. |
| `shape_time_series_metadata()` | `shape_time_series_metadata( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape time series metadata. Transforms raw provider records into time series metadata structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. operation. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. |
| `shape_latest_values()` | `shape_latest_values( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape latest values. Transforms raw provider records into latest values structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for USGS water services records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( collection: str, params: Dict[str, Any], time: int = 20 ) -> Dict[str, Any] \| None` | Request USGS water services records. Sends a provider request for USGS water services records after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_monitoring_locations()` | `fetch_monitoring_locations( monitoring_location_id: str = '', state_code: str = '', county_code: str = '', site_type: str = '', limit: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch monitoring locations. Retrieves monitoring locations using validated arguments and the stored USGSWaterData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. locations operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_time_series_metadata()` | `fetch_time_series_metadata( monitoring_location_id: str = '', parameter_code: str = '', limit: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch time series metadata. Retrieves time series metadata using validated arguments and the stored USGSWaterData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. metadata operation. operation. |
| `fetch_latest_continuous()` | `fetch_latest_continuous( monitoring_location_id: str = '', parameter_code: str = '', limit: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch latest continuous. Retrieves latest continuous using validated arguments and the stored USGSWaterData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. continuous operation. operation. |
| `fetch_latest_daily()` | `fetch_latest_daily( monitoring_location_id: str = '', parameter_code: str = '', limit: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch latest daily. Retrieves latest daily using validated arguments and the stored USGSWaterData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. daily operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'monitoring-locations', monitoring_location_id: str = '', state_code: str = '', county_code: str = '', site_type: str = '', parameter_code: str = '', limit: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch USGS water services records. Retrieves USGS water services records using validated arguments and the stored USGSWaterData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `USGSTheNationalMap`

U S G S The National Map fetcher. Coordinates USGS National Map datasets and products within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_endpoint()` | `validate_endpoint( endpoint: str ) -> str` | Validate endpoint. Validates and normalizes endpoint before it is used in USGS National Map datasets and products. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated endpoint value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_max_items()` | `validate_max_items( max_items: int ) -> int` | Validate max items. Validates and normalizes max items before it is used in USGS National Map datasets and products. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated max items value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_offset()` | `validate_offset( offset: int ) -> int` | Validate offset. Validates and normalizes offset before it is used in USGS National Map datasets and products. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated offset value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_bbox()` | `validate_bbox( bbox: str ) -> str` | Validate bbox. Validates and normalizes bbox before it is used in USGS National Map datasets and products. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated bbox value. Error: Re-raised after the original exception is wrapped and written to the application |
| `coalesce_records()` | `coalesce_records( payload: Any ) -> List[Dict[str, Any]]` | Coalesce records. Performs the coalesce records operation for USGS National Map datasets and products. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[Dict[str, Any]]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_dataset_rows()` | `shape_dataset_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape dataset rows. Transforms raw provider records into dataset rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_product_rows()` | `shape_product_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape product rows. Transforms raw provider records into product rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for USGS National Map datasets and products. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( endpoint: str, params: Dict[str, Any], time: int = 20 ) -> Dict[str, Any] \| None` | Request USGS National Map datasets and products. Sends a provider request for USGS National Map datasets and products after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_datasets()` | `fetch_datasets( time: int = 20 ) -> Dict[str, Any] \| None` | Fetch datasets. Retrieves datasets using validated arguments and the stored USGSTheNationalMap runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_products()` | `fetch_products( dataset: str = '', q: str = '', bbox: str = '', prod_formats: str = '', max_items: int = 25, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch products. Retrieves products using validated arguments and the stored USGSTheNationalMap runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'products', dataset: str = '', q: str = '', bbox: str = '', prod_formats: str = '', max_items: int = 25, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch USGS National Map datasets and products. Retrieves USGS National Map datasets and products using validated arguments and the stored USGSTheNationalMap runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `USGSScienceBase`

U S G S Science Base fetcher. Coordinates USGS ScienceBase items and catalog records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_endpoint()` | `validate_endpoint( endpoint: str ) -> str` | Validate endpoint. Validates and normalizes endpoint before it is used in USGS ScienceBase items and catalog records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated endpoint value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_max_items()` | `validate_max_items( max_items: int ) -> int` | Validate max items. Validates and normalizes max items before it is used in USGS ScienceBase items and catalog records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated max items value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_offset()` | `validate_offset( offset: int ) -> int` | Validate offset. Validates and normalizes offset before it is used in USGS ScienceBase items and catalog records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated offset value. Error: Re-raised after the original exception is wrapped and written to the application |
| `coalesce_records()` | `coalesce_records( payload: Any ) -> List[Dict[str, Any]]` | Coalesce records. Performs the coalesce records operation for USGS ScienceBase items and catalog records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[Dict[str, Any]]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_single_item()` | `shape_single_item( item: Dict[str, Any] ) -> Dict[str, Any]` | Shape single item. Transforms raw provider records into single item structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. Dict[str, Any]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_item_rows()` | `shape_item_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape item rows. Transforms raw provider records into item rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for USGS ScienceBase items and catalog records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( endpoint: str, params: Optional[Dict[str, Any]] = None, time: int = 20 ) -> Dict[str, Any] \| None` | Request USGS ScienceBase items and catalog records. Sends a provider request for USGS ScienceBase items and catalog records after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_items()` | `fetch_items( q: str = '', max_items: int = 25, offset: int = 0, fields: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch items. Retrieves items using validated arguments and the stored USGSScienceBase runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_item()` | `fetch_item( item_id: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch item. Retrieves item using validated arguments and the stored USGSScienceBase runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'items', q: str = '', item_id: str = '', max_items: int = 25, offset: int = 0, fields: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch USGS ScienceBase items and catalog records. Retrieves USGS ScienceBase items and catalog records using validated arguments and the stored USGSScienceBase runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `AirNow`

Air Now fetcher. Coordinates AirNow current and forecast air quality data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `request()` | `request( endpoint: str, params: Optional[Dict[str, Any]] = None, time: int = 20 ) -> Dict[str, Any] \| None` | Request AirNow current and forecast air quality data. Sends a provider request for AirNow current and forecast air quality data after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `shape_rows()` | `shape_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape rows. Transforms raw provider records into rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for AirNow current and forecast air quality data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response(  ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `fetch_current_zip()` | `fetch_current_zip( zip_code: str, distance: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch current zip. Retrieves current zip using validated arguments and the stored AirNow runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_current_latlon()` | `fetch_current_latlon( latitude: float, longitude: float, distance: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch current latlon. Retrieves current latlon using validated arguments and the stored AirNow runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_forecast_zip()` | `fetch_forecast_zip( zip_code: str, date: str, distance: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch forecast zip. Retrieves forecast zip using validated arguments and the stored AirNow runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_forecast_latlon()` | `fetch_forecast_latlon( latitude: float, longitude: float, date: str, distance: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch forecast latlon. Retrieves forecast latlon using validated arguments and the stored AirNow runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'current-zip', zip_code: str = '', latitude: float \| None = None, longitude: float \| None = None, date: str = '', distance: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch AirNow current and forecast air quality data. Retrieves AirNow current and forecast air quality data using validated arguments and the stored AirNow runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `ClimateData`

Climate Data fetcher. Coordinates NOAA climate dataset and data records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_limit()` | `validate_limit( limit: int ) -> int` | Validate limit. Validates and normalizes limit before it is used in NOAA climate dataset and data records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated limit value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_offset()` | `validate_offset( offset: int ) -> int` | Validate offset. Validates and normalizes offset before it is used in NOAA climate dataset and data records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated offset value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_date_range()` | `validate_date_range( start_date: str, end_date: str ) -> Tuple[str, str]` | Validate date range. Validates and normalizes date range before it is used in NOAA climate dataset and data records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. Tuple[str, str]: Validated date range value. Error: Re-raised after the original exception is wrapped and written to the application |
| `coalesce_records()` | `coalesce_records( payload: Any ) -> List[Dict[str, Any]]` | Coalesce records. Performs the coalesce records operation for NOAA climate dataset and data records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[Dict[str, Any]]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_dataset_rows()` | `shape_dataset_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape dataset rows. Transforms raw provider records into dataset rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_data_rows()` | `shape_data_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape data rows. Transforms raw provider records into data rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for NOAA climate dataset and data records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( url: str, params: Dict[str, Any], time: int = 20 ) -> Dict[str, Any] \| None` | Request NOAA climate dataset and data records. Sends a provider request for NOAA climate dataset and data records after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_datasets()` | `fetch_datasets( keyword: str = '', start_date: str = '', end_date: str = '', limit: int = 25, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch datasets. Retrieves datasets using validated arguments and the stored ClimateData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_data()` | `fetch_data( dataset: str, start_date: str, end_date: str, stations: str = '', data_types: str = '', limit: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch data. Retrieves data using validated arguments and the stored ClimateData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'datasets', keyword: str = '', dataset: str = '', start_date: str = '', end_date: str = '', stations: str = '', data_types: str = '', limit: int = 25, offset: int = 0, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch NOAA climate dataset and data records. Retrieves NOAA climate dataset and data records using validated arguments and the stored ClimateData runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `EoNet`

Eo Net fetcher. Coordinates NASA EONET environmental event data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_endpoint()` | `validate_endpoint( endpoint: str ) -> str` | Validate endpoint. Validates and normalizes endpoint before it is used in NASA EONET environmental event data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated endpoint value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_status()` | `validate_status( status: str ) -> str` | Validate status. Validates and normalizes status before it is used in NASA EONET environmental event data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated status value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_limit()` | `validate_limit( limit: int ) -> int` | Validate limit. Validates and normalizes limit before it is used in NASA EONET environmental event data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated limit value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_days()` | `validate_days( days: int ) -> int` | Validate days. Validates and normalizes days before it is used in NASA EONET environmental event data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated days value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_bbox()` | `validate_bbox( bbox: str ) -> str` | Validate bbox. Validates and normalizes bbox before it is used in NASA EONET environmental event data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated bbox value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_date_pair()` | `validate_date_pair( start_date: str = '', end_date: str = '' ) -> Tuple[str, str]` | Validate date pair. Validates and normalizes date pair before it is used in NASA EONET environmental event data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. Tuple[str, str]: Validated date pair value. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_event_rows()` | `shape_event_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape event rows. Transforms raw provider records into event rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_category_rows()` | `shape_category_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape category rows. Transforms raw provider records into category rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for NASA EONET environmental event data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( endpoint: str, params: Optional[Dict[str, Any]] = None, time: int = 20 ) -> Dict[str, Any] \| None` | Request NASA EONET environmental event data. Sends a provider request for NASA EONET environmental event data after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_events()` | `fetch_events( source: str = '', category: str = '', status: str = 'open', limit: int = 25, days: int = 30, start_date: str = '', end_date: str = '', bbox: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch events. Retrieves events using validated arguments and the stored EoNet runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_categories()` | `fetch_categories( time: int = 20 ) -> Dict[str, Any] \| None` | Fetch categories. Retrieves categories using validated arguments and the stored EoNet runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'events', source: str = '', category: str = '', status: str = 'open', limit: int = 25, days: int = 30, start_date: str = '', end_date: str = '', bbox: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch NASA EONET environmental event data. Retrieves NASA EONET environmental event data using validated arguments and the stored EoNet runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `EnviroFacts`

Enviro Facts fetcher. Coordinates EPA Envirofacts table and facility records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_table_name()` | `validate_table_name( table_name: str ) -> str` | Validate table name. Validates and normalizes table name before it is used in EPA Envirofacts table and facility records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated table name value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_state_code()` | `validate_state_code( state_code: str = '' ) -> str` | Validate state code. Validates and normalizes state code before it is used in EPA Envirofacts table and facility records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated state code value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_limit()` | `validate_limit( limit: int ) -> int` | Validate limit. Validates and normalizes limit before it is used in EPA Envirofacts table and facility records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated limit value. Error: Re-raised after the original exception is wrapped and written to the application |
| `resolve_table_path()` | `resolve_table_path( table_name: str, state_code: str = '', facility_name: str = '', limit: int = 25 ) -> str` | Resolve table path. Performs the resolve table path operation for EPA Envirofacts table and facility records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_rows()` | `shape_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape rows. Transforms raw provider records into rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for EPA Envirofacts table and facility records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( url: str, time: int = 20 ) -> Dict[str, Any] \| None` | Request EPA Envirofacts table and facility records. Sends a provider request for EPA Envirofacts table and facility records after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_table()` | `fetch_table( table_name: str, state_code: str = '', facility_name: str = '', limit: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch table. Retrieves table using validated arguments and the stored EnviroFacts runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( table_name: str = 'TRI_FACILITY', state_code: str = '', facility_name: str = '', limit: int = 25, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch EPA Envirofacts table and facility records. Retrieves EPA Envirofacts table and facility records using validated arguments and the stored EnviroFacts runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `TidesAndCurrents`

Tides And Currents fetcher. Coordinates NOAA tides, currents, and station data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_mode()` | `validate_mode( mode: str ) -> str` | Validate mode. Validates and normalizes mode before it is used in NOAA tides, currents, and station data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated mode value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_station_id()` | `validate_station_id( station_id: str ) -> str` | Validate station id. Validates and normalizes station id before it is used in NOAA tides, currents, and station data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated station id value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_date_range()` | `validate_date_range( begin_date: str, end_date: str ) -> Tuple[str, str]` | Validate date range. Validates and normalizes date range before it is used in NOAA tides, currents, and station data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. Tuple[str, str]: Validated date range value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_datum()` | `validate_datum( datum: str ) -> str` | Validate datum. Validates and normalizes datum before it is used in NOAA tides, currents, and station data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated datum value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_units()` | `validate_units( units: str ) -> str` | Validate units. Validates and normalizes units before it is used in NOAA tides, currents, and station data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated units value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_time_zone()` | `validate_time_zone( time_zone: str ) -> str` | Validate time zone. Validates and normalizes time zone before it is used in NOAA tides, currents, and station data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated time zone value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_interval()` | `validate_interval( interval: str ) -> str` | Validate interval. Validates and normalizes interval before it is used in NOAA tides, currents, and station data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated interval value. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_station_rows()` | `shape_station_rows( payload: Dict[str, Any] ) -> List[Dict[str, Any]]` | Shape station rows. Transforms raw provider records into station rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_data_rows()` | `shape_data_rows( payload: Dict[str, Any] ) -> List[Dict[str, Any]]` | Shape data rows. Transforms raw provider records into data rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for NOAA tides, currents, and station data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( url: str, params: Optional[Dict[str, Any]] = None, time: int = 20 ) -> Dict[str, Any] \| None` | Request NOAA tides, currents, and station data. Sends a provider request for NOAA tides, currents, and station data after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_station()` | `fetch_station( station_id: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch station. Retrieves station using validated arguments and the stored TidesAndCurrents runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_water_level()` | `fetch_water_level( station_id: str, begin_date: str, end_date: str, datum: str = 'MLLW', units: str = 'metric', time_zone: str = 'gmt', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch water level. Retrieves water level using validated arguments and the stored TidesAndCurrents runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_tide_predictions()` | `fetch_tide_predictions( station_id: str, begin_date: str, end_date: str, datum: str = 'MLLW', units: str = 'metric', time_zone: str = 'gmt', interval: str = 'hilo', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch tide predictions. Retrieves tide predictions using validated arguments and the stored TidesAndCurrents runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'water-level', station_id: str = '', begin_date: str = '', end_date: str = '', datum: str = 'MLLW', units: str = 'metric', time_zone: str = 'gmt', interval: str = 'hilo', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch NOAA tides, currents, and station data. Retrieves NOAA tides, currents, and station data using validated arguments and the stored TidesAndCurrents runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `UvIndex`

UV Index fetcher. Coordinates EPA UV Index current and forecast data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_mode()` | `validate_mode( mode: str ) -> str` | Validate mode. Validates and normalizes mode before it is used in EPA UV Index current and forecast data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated mode value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_zip_code()` | `validate_zip_code( zip_code: str ) -> str` | Validate zip code. Validates and normalizes zip code before it is used in EPA UV Index current and forecast data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated zip code value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_city()` | `validate_city( city: str ) -> str` | Validate city. Validates and normalizes city before it is used in EPA UV Index current and forecast data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated city value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_state()` | `validate_state( state: str ) -> str` | Validate state. Validates and normalizes state before it is used in EPA UV Index current and forecast data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated state value. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_rows()` | `shape_rows( records: List[Dict[str, Any]] ) -> List[Dict[str, Any]]` | Shape rows. Transforms raw provider records into rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for EPA UV Index current and forecast data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( url: str, params: Dict[str, Any], time: int = 20 ) -> Dict[str, Any] \| None` | Request EPA UV Index current and forecast data. Sends a provider request for EPA UV Index current and forecast data after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_daily_zip()` | `fetch_daily_zip( zip_code: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch daily zip. Retrieves daily zip using validated arguments and the stored UvIndex runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_daily_city_state()` | `fetch_daily_city_state( city: str, state: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch daily city state. Retrieves daily city state using validated arguments and the stored UvIndex runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_hourly_zip()` | `fetch_hourly_zip( zip_code: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch hourly zip. Retrieves hourly zip using validated arguments and the stored UvIndex runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_hourly_city_state()` | `fetch_hourly_city_state( city: str, state: str, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch hourly city state. Retrieves hourly city state using validated arguments and the stored UvIndex runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'daily-zip', zip_code: str = '', city: str = '', state: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch EPA UV Index current and forecast data. Retrieves EPA UV Index current and forecast data using validated arguments and the stored UvIndex runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `PurpleAir`

Purple Air fetcher. Coordinates PurpleAir sensor and air quality records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_api_key()` | `validate_api_key(  ) -> str` | Validate api key. Validates and normalizes api key before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated api key value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_mode()` | `validate_mode( mode: str ) -> str` | Validate mode. Validates and normalizes mode before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated mode value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_endpoint()` | `validate_endpoint( endpoint: str ) -> str` | Validate endpoint. Validates and normalizes endpoint before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated endpoint value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_sensor_index()` | `validate_sensor_index( sensor_index: int ) -> int` | Validate sensor index. Validates and normalizes sensor index before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated sensor index value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_longitude()` | `validate_longitude( name: str, value: float ) -> float` | Validate longitude. Validates and normalizes longitude before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. float: Validated longitude value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_latitude()` | `validate_latitude( name: str, value: float ) -> float` | Validate latitude. Validates and normalizes latitude before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. float: Validated latitude value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_bbox()` | `validate_bbox( nwlng: float, nwlat: float, selng: float, selat: float ) -> Tuple[float, float, float, float]` | Validate bbox. Validates and normalizes bbox before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. Tuple[float, float, float, float]: Validated bbox value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_location_type()` | `validate_location_type( location_type: int ) -> int` | Validate location type. Validates and normalizes location type before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated location type value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_non_negative_integer()` | `validate_non_negative_integer( name: str, value: int ) -> int` | Validate non negative integer. Validates and normalizes non negative integer before it is used in PurpleAir sensor and air quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated non negative integer value. Error: Re-raised after the original exception is wrapped and written to the application |
| `normalize_fields()` | `normalize_fields( fields: str, default_fields: str ) -> str` | Normalize fields. Converts fields into the canonical format expected by the PurpleAir request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. str: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_sensor_list_rows()` | `shape_sensor_list_rows( payload: Dict[str, Any] ) -> List[Dict[str, Any]]` | Shape sensor list rows. Transforms raw provider records into sensor list rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_sensor_detail_rows()` | `shape_sensor_detail_rows( payload: Dict[str, Any] ) -> List[Dict[str, Any]]` | Shape sensor detail rows. Transforms raw provider records into sensor detail rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for PurpleAir sensor and air quality records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]], params: Dict[str, Any] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request()` | `request( endpoint: str, params: Optional[Dict[str, Any]] = None, time: int = 20 ) -> Dict[str, Any] \| None` | Request PurpleAir sensor and air quality records. Sends a provider request for PurpleAir sensor and air quality records after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_sensors()` | `fetch_sensors( nwlng: float, nwlat: float, selng: float, selat: float, location_type: int = 0, max_age: int = 0, modified_since: int = 0, fields: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch sensors. Retrieves sensors using validated arguments and the stored PurpleAir runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_sensor()` | `fetch_sensor( sensor_index: int, fields: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch sensor. Retrieves sensor using validated arguments and the stored PurpleAir runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'sensors', sensor_index: int = None, nwlng: float \| None = None, nwlat: float \| None = None, selng: float \| None = None, selat: float \| None = None, location_type: int = 0, max_age: int = 0, modified_since: int = 0, fields: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch PurpleAir sensor and air quality records. Retrieves PurpleAir sensor and air quality records using validated arguments and the stored PurpleAir runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `OpenAQ`

Open A Q fetcher. Coordinates OpenAQ location, measurement, and air-quality records within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_api_key()` | `validate_api_key(  ) -> str` | Validate api key. Validates and normalizes api key before it is used in OpenAQ location, measurement, and air-quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated api key value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_mode()` | `validate_mode( mode: str ) -> str` | Validate mode. Validates and normalizes mode before it is used in OpenAQ location, measurement, and air-quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated mode value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_endpoint()` | `validate_endpoint( endpoint: str ) -> str` | Validate endpoint. Validates and normalizes endpoint before it is used in OpenAQ location, measurement, and air-quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated endpoint value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_positive_integer()` | `validate_positive_integer( name: str, value: Any, maximum: int \| None = None ) -> int` | Validate positive integer. Validates and normalizes positive integer before it is used in OpenAQ location, measurement, and air-quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated positive integer value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_non_negative_integer()` | `validate_non_negative_integer( name: str, value: Any ) -> int` | Validate non negative integer. Validates and normalizes non negative integer before it is used in OpenAQ location, measurement, and air-quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated non negative integer value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_coordinates()` | `validate_coordinates( coordinates: str = '' ) -> str` | Validate coordinates. Validates and normalizes coordinates before it is used in OpenAQ location, measurement, and air-quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated coordinates value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_radius()` | `validate_radius( radius: int ) -> int` | Validate radius. Validates and normalizes radius before it is used in OpenAQ location, measurement, and air-quality records. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated radius value. Error: Re-raised after the original exception is wrapped and written to the application |
| `coalesce_results()` | `coalesce_results( payload: Any ) -> List[Dict[str, Any]]` | Coalesce results. Performs the coalesce results operation for OpenAQ location, measurement, and air-quality records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[Dict[str, Any]]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_resource_rows()` | `shape_resource_rows( payload: Any, resource_name: str ) -> List[Dict[str, Any]]` | Shape resource rows. Transforms raw provider records into resource rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_location_rows()` | `shape_location_rows( payload: Any ) -> List[Dict[str, Any]]` | Shape location rows. Transforms raw provider records into location rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `shape_latest_rows()` | `shape_latest_rows( payload: Any ) -> List[Dict[str, Any]]` | Shape latest rows. Transforms raw provider records into latest rows structures with consistent keys and row-oriented values. The method prepares service responses for display, export, analysis, or downstream processing without changing the provider request contract. List[Dict[str, Any]]: Normalized rows or summary structures created from provider response records. Error: Re-raised after the original exception is wrapped and written to the application |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for OpenAQ location, measurement, and air-quality records. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `package_response()` | `package_response( rows: List[Dict[str, Any]], params: Optional[Dict[str, Any]] = None ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. operation. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `request()` | `request( endpoint: str, params: Optional[Dict[str, Any]] = None, time: int = 20 ) -> Dict[str, Any] \| None` | Request OpenAQ location, measurement, and air-quality records. Sends a provider request for OpenAQ location, measurement, and air-quality records after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. operation. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_countries()` | `fetch_countries( providers_id: str = '', parameters_id: str = '', limit: int = 100, page: int = 1, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch countries. Retrieves countries using validated arguments and the stored OpenAQ runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_providers()` | `fetch_providers( limit: int = 100, page: int = 1, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch providers. Retrieves providers using validated arguments and the stored OpenAQ runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_parameters()` | `fetch_parameters( limit: int = 100, page: int = 1, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch parameters. Retrieves parameters using validated arguments and the stored OpenAQ runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_parameter_latest()` | `fetch_parameter_latest( parameter_id: int, limit: int = 100, page: int = 1, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch parameter latest. Retrieves parameter latest using validated arguments and the stored OpenAQ runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_locations()` | `fetch_locations( country_id: int = None, coordinates: str = '', radius: int = 25000, providers_id: str = '', parameters_id: str = '', limit: int = 25, page: int = 1, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch locations. Retrieves locations using validated arguments and the stored OpenAQ runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_latest()` | `fetch_latest( location_id: int, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch latest. Retrieves latest using validated arguments and the stored OpenAQ runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'locations', location_id: int = None, parameter_id: int = None, country_id: int = None, coordinates: str = '', radius: int = 25000, providers_id: str = '', parameters_id: str = '', limit: int = 25, page: int = 1, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch OpenAQ location, measurement, and air-quality records. Retrieves OpenAQ location, measurement, and air-quality records using validated arguments and the stored OpenAQ runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `Firms`

Firms fetcher. Coordinates NASA FIRMS active fire data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_map_key()` | `validate_map_key(  ) -> str` | Validate map key. Validates and normalizes map key before it is used in NASA FIRMS active fire data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated map key value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_mode()` | `validate_mode( mode: str ) -> str` | Validate mode. Validates and normalizes mode before it is used in NASA FIRMS active fire data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated mode value. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `validate_source()` | `validate_source( source: str ) -> str` | Validate source. Validates and normalizes source before it is used in NASA FIRMS active fire data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated source value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_sensor()` | `validate_sensor( sensor: str ) -> str` | Validate sensor. Validates and normalizes sensor before it is used in NASA FIRMS active fire data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated sensor value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_day_range()` | `validate_day_range( day_range: int ) -> int` | Validate day range. Validates and normalizes day range before it is used in NASA FIRMS active fire data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated day range value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_date()` | `validate_date( date: str = '' ) -> str` | Validate date. Validates and normalizes date before it is used in NASA FIRMS active fire data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated date value. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `validate_area_coordinates()` | `validate_area_coordinates( area_coordinates: str = 'world' ) -> str` | Validate area coordinates. Validates and normalizes area coordinates before it is used in NASA FIRMS active fire data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. operation. str: Validated area coordinates value. Error: Re-raised after the original exception is wrapped and written to the application |
| `csv_to_rows()` | `csv_to_rows( csv_text: str ) -> List[Dict[str, Any]]` | CSV to rows. Performs the csv to rows operation for NASA FIRMS active fire data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. List[Dict[str, Any]]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `summarize_rows()` | `summarize_rows( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Summarize rows. Performs the summarize rows operation for NASA FIRMS active fire data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `package_response()` | `package_response( rows: List[Dict[str, Any]] ) -> Dict[str, Any]` | Package response. Packages response into the app-facing response structure used by Fonky callers. The method combines stored mode, URL, parameters, and payload state into a normalized dictionary suitable for display, export, or tool output. Dict[str, Any]: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `request_csv()` | `request_csv( url: str, time: int = 20 ) -> Dict[str, Any] \| None` | Request CSV. Performs the request csv operation for NASA FIRMS active fire data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Dict[str, Any] \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `fetch_area()` | `fetch_area( source: str, area_coordinates: str = 'world', day_range: int = 1, date: str = '', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch area. Retrieves area using validated arguments and the stored Firms runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_data_availability()` | `fetch_data_availability( sensor: str = 'ALL', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch data availability. Retrieves data availability using validated arguments and the stored Firms runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'area', source: str = 'VIIRS_SNPP_NRT', area_coordinates: str = 'world', day_range: int = 1, date: str = '', sensor: str = 'ALL', time: int = 20 ) -> Dict[str, Any] \| None` | Fetch NASA FIRMS active fire data. Retrieves NASA FIRMS active fire data using validated arguments and the stored Firms runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## `OpenSky`

Open Sky fetcher. Coordinates OpenSky Network aircraft, airport, and state-vector data within Fonky. The class stores provider configuration, request parameters, response payloads, and normalized result state so callers can access the service through a consistent fetcher interface.

| Method | Signature | Purpose |
|---|---|---|
| `validate_mode()` | `validate_mode( mode: str ) -> str` | Validate mode. Validates and normalizes mode before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated mode value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_endpoint()` | `validate_endpoint( endpoint: str ) -> str` | Validate endpoint. Validates and normalizes endpoint before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated endpoint value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_icao24()` | `validate_icao24( icao24: str ) -> str` | Validate icao24. Validates and normalizes icao24 before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated icao24 value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_airport()` | `validate_airport( airport: str ) -> str` | Validate airport. Validates and normalizes airport before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. str: Validated airport value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_epoch()` | `validate_epoch( name: str, value: Any ) -> int` | Validate epoch. Validates and normalizes epoch before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. int: Validated epoch value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_time_range()` | `validate_time_range( begin: int, end: int ) -> Tuple[int, int]` | Validate time range. Validates and normalizes time range before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. Tuple[int, int]: Validated time range value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_latitude()` | `validate_latitude( name: str, value: Any ) -> float` | Validate latitude. Validates and normalizes latitude before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. float: Validated latitude value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_longitude()` | `validate_longitude( name: str, value: Any ) -> float` | Validate longitude. Validates and normalizes longitude before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. float: Validated longitude value. Error: Re-raised after the original exception is wrapped and written to the application |
| `validate_bbox()` | `validate_bbox( lamin: float, lomin: float, lamax: float, lomax: float ) -> Tuple[float, float, float, float]` | Validate bbox. Validates and normalizes bbox before it is used in OpenSky Network aircraft, airport, and state-vector data. The method converts incoming values to the expected representation, enforces visible range or format constraints, and returns the checked value for request construction. Tuple[float, float, float, float]: Validated bbox value. Error: Re-raised after the original exception is wrapped and written to the application |
| `assign_credentials()` | `assign_credentials( client_id: str = None, client_secret: str = None ) -> None` | Assign credentials. Performs the assign credentials operation for OpenSky Network aircraft, airport, and state-vector data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. Error: Re-raised after the original exception is wrapped and written to the application logger. |
| `authenticate()` | `authenticate(  ) -> str \| None` | Authenticate. Performs the authenticate operation for OpenSky Network aircraft, airport, and state-vector data. The method uses the class runtime state and supplied arguments to prepare, transform, dispatch, or package data for the broader Fonky fetching workflow. str \| None: Result produced by the operation. Error: Re-raised after the original exception is wrapped and written to the application |
| `request()` | `request( endpoint: str, params: Dict[str, Any] \| None = None, client_id: str = None, client_secret: str = None ) -> Any` | Request OpenSky Network aircraft, airport, and state-vector data. Sends a provider request for OpenSky Network aircraft, airport, and state-vector data after request parameters have been validated or prepared by the caller. The method stores request, response, and payload state on the instance so subsequent methods can package or inspect the result consistently. operation. object: Normalized provider response, result payload, or document collection returned by |
| `normalize_states()` | `normalize_states( payload: Dict[str, Any] \| None ) -> Dict[str, Any] \| None` | Normalize states. Converts states into the canonical format expected by the OpenSky request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. Dict[str, Any] \| None: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `normalize_flights()` | `normalize_flights( payload: List[Dict[str, Any]] \| None, mode: str ) -> Dict[str, Any] \| None` | Normalize flights. Converts flights into the canonical format expected by the OpenSky request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. operation. Dict[str, Any] \| None: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `normalize_track()` | `normalize_track( payload: Dict[str, Any] \| None ) -> Dict[str, Any] \| None` | Normalize track. Converts track into the canonical format expected by the OpenSky request workflow. The method standardizes caller input before it is used in endpoint paths, query parameters, filters, or response processing. Dict[str, Any] \| None: Canonical value prepared for request construction or response processing. Error: Re-raised after the original exception is wrapped and written to the application |
| `fetch_states()` | `fetch_states( icao24: str = '', time_value: int = None, lamin: float \| None = None, lomin: float \| None = None, lamax: float \| None = None, lomax: float \| None = None, extended: bool = False, client_id: str = None, client_secret: str = None ) -> Dict[str, Any] \| None` | Fetch states. Retrieves states using validated arguments and the stored OpenSky runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_flights_aircraft()` | `fetch_flights_aircraft( icao24: str, begin: int, end: int, client_id: str = None, client_secret: str = None ) -> Dict[str, Any] \| None` | Fetch flights aircraft. Retrieves flights aircraft using validated arguments and the stored OpenSky runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_arrivals_airport()` | `fetch_arrivals_airport( airport: str, begin: int, end: int, client_id: str = None, client_secret: str = None ) -> Dict[str, Any] \| None` | Fetch arrivals airport. Retrieves arrivals airport using validated arguments and the stored OpenSky runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_departures_airport()` | `fetch_departures_airport( airport: str, begin: int, end: int, client_id: str = None, client_secret: str = None ) -> Dict[str, Any] \| None` | Fetch departures airport. Retrieves departures airport using validated arguments and the stored OpenSky runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch_track_aircraft()` | `fetch_track_aircraft( icao24: str, time_value: int = None, client_id: str = None, client_secret: str = None ) -> Dict[str, Any] \| None` | Fetch track aircraft. Retrieves track aircraft using validated arguments and the stored OpenSky runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis or tool execution. Dict[str, Any] \| None: Normalized provider response, result payload, or document |
| `fetch()` | `fetch( mode: str = 'states_bbox', icao24: str = '', airport: str = '', begin: int = None, end: int = None, time_value: int = None, lamin: float \| None = None, lomin: float \| None = None, lamax: float \| None = None, lomax: float \| None = None, extended: bool = False, client_id: str = None, client_secret: str = None, time: int = 20 ) -> Dict[str, Any] \| None` | Fetch OpenSky Network aircraft, airport, and state-vector data. Retrieves OpenSky Network aircraft, airport, and state-vector data using validated arguments and the stored OpenSky runtime configuration. The method assembles request parameters, executes the provider call or dispatches to a specialized helper, records response state when applicable, and returns a normalized payload for downstream analysis |
| `create_schema()` | `create_schema( function: str, tool: str, description: str, parameters: dict, required: list[str] ) -> Dict[str, str] \| None` | Create an AI tool schema. Builds a compact function schema for exposing a fetcher operation to AI tool-calling workflows. The method combines the public function name, service label, description, parameter schema, and required-field list into the structure consumed by downstream tool registries. schema. Dict[str, str] \| None: Function schema dictionary used by downstream AI tool-calling |

## Module Functions

| Function | Signature | Purpose |
|---|---|---|
| `throw_if()` | `throw_if( name: str, value: object ) -> None` | Throw if. Validates that a required argument contains a usable value so failures occur before provider, filesystem, or parsing work begins. None: This method updates instance state or validates input and does not return a value. ValueError: Raised when the method cannot satisfy its documented value requirement. |
| `encode_image()` | `encode_image( path: str ) -> str` | Encode image. Reads an image file from disk and returns a base64-encoded string representation of its bytes. The function validates that a path was supplied before reading the file so image payloads can be embedded in request or tool workflows. str: Base64-encoded image data. Exception: Raised when validation or provider-specific failure conditions are encountered. |
